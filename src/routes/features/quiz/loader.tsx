const Loader = () => {
    return (
        <section id="center">
            <h1>Loading...</h1>
            <p>The quiz is loading, please wait.</p>
        </section>
    );
};

export default Loader;